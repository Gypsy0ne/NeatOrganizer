<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tasksWidgetContainer"
    android:layout_width="match_parent"
    android:background="@drawable/task_widget_background"
    android:layout_height="match_parent">


    <TextView
        android:id="@+id/taskGroupTitle"
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:layout_marginHorizontal="@dimen/medium_space"
        android:autoSizeMaxTextSize="@dimen/regular_text_size"
        android:autoSizeMinTextSize="@dimen/small_text_size"
        android:autoSizeTextType="uniform"
        android:gravity="center_vertical" />

    <ListView
        android:id="@+id/tasksList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="0.5dp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="0.5dp"
        android:paddingHorizontal="@dimen/regular_space" />

    <TextView
        android:id="@+id/emptyView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="no items inside"
        android:textColor="@color/white" />


</FrameLayout>