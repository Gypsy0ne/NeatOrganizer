package one.gypsy.neatorganizer.model

import android.graphics.Bitmap
import java.util.*

data class Person(val name: String/*, val photoThumbnail: Bitmap*/, val lastInteraction: Int, val dateOfBirth: Date)